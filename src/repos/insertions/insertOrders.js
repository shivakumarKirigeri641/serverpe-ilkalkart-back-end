const { connectDB } = require("../../database/connectDB");
const pool = connectDB();
const insertOrders = async (user_and_addressdata, order_id) => {
  try {
  } catch (err) {}
};
module.exports = insertOrders;
